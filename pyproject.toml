[project]
name = "moonpayradar"
version = "0.1.0"
description = "X/Twitter promo + airdrop radar for vendor accounts"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "twikit>=2.3.3",
]
